# Shorts — AI 쇼츠 자동 생성 시스템

Python 파이프라인 + GitHub Actions. 매일 1편 생성 → 텔레그램 1탭 승인 →
YouTube 업로드. 수익화 목적.

---

## 🔴 세션을 시작하면 가장 먼저 할 일

**`docs/STATE.md`를 읽어라.** 지금 어디까지 됐고 다음에 뭘 해야 하는지가 거기 있다.

이 프로젝트는 **여러 PC에서 번갈아 작업한다.** Claude의 로컬 메모리는 PC 사이를
넘어오지 않으므로, PC 간에 전달되는 상태는 오직 git에 커밋된 문서뿐이다
(설계 문서 6절이 이 저장소를 공유 컨텍스트로 정한 이유다).

## 문서 지도

| 파일 | 무엇이 있나 |
|---|---|
| `docs/STATE.md` | **현재 상태와 다음 할 일.** 매 세션 마무리에 갱신 |
| `todo.md` | 작업 큐. 완료 시 `[x]`로 체크하고 **무엇을 어떻게 검증했는지** 남긴다 |
| `README.md` | 셋업 절차, 비용표, Actions 사용시간 계산, 정책 근거 |
| `docs/superpowers/specs/2026-08-31-*.md` | 설계 근거 — 니치 선정 리서치, 정책 리스크 |
| `docs/superpowers/plans/2026-08-31-*.md` | 구현 계획. 13개 Task 전부 완료 |

## 반드시 알아야 할 함정

- **계획서에 적힌 외부 API 형태를 믿지 마라.** 구현 중 Gemini·MiniMax 양쪽 다
  실제와 달랐고, 그대로 옮겼으면 첫 실행에서 죽었다. 외부 API를 건드릴 땐
  라이브 문서를 먼저 확인한다. → `docs/STATE.md` "최근에 알게 된 것"
- **테스트 98건은 전부 모킹이다.** API 스키마가 틀려도 초록불이 뜬다.
  돈이 나가는 경로는 실제 키로 한 번 돌려 봐야 검증된 것이다.
- **`pytest.ini`의 `pythonpath = src`는 pytest 전용이다.** 직접 실행할 땐
  `PYTHONPATH=src python -m pipeline.orchestrator`.
- **ffmpeg drawtext는 `fontfile=` 없이 실패한다.** 리눅스에서도
  `fonts-dejavu-core`를 따로 깔아야 한다.
- **`assets/music/`이 비어 있으면 렌더가 불가능하다.** 트랙은 수동으로만 채운다.
- 실행은 돈을 쓴다. 영상 1편 $3~5. 함부로 `run_pipeline()`을 돌리지 말 것.

## 작업 규칙

- `todo.md`가 작업 큐다. 완료하면 체크하고 **무엇을 어떻게 검증했는지**까지 적는다.
- 코드를 고쳤으면 `python -m pytest`를 통과시킨 뒤 커밋한다.
- 모킹 테스트를 쓸 땐 **고치기 전 코드로 되돌렸을 때 실제로 빨간불이 되는지**
  확인한다. 헛도는 테스트를 거르는 가장 싼 방법이다.
- 커밋 메시지는 한국어로, **왜 그렇게 했는지**를 남긴다.
